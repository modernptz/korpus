// Generated by CoffeeScript 1.4.0
(function() {
  var angular, root, thisApp;

  root = typeof global !== "undefined" && global !== null ? global : window;

  angular = root.angular;

  thisApp = angular.module("KorpusClient", ['ngCookies', 'forms']).config([
    '$routeProvider', '$locationProvider', function($routeProvider, $locationProvider) {
      $locationProvider.html5Mode(true);
      return $routeProvider.when("/", {
        redirectTo: "/forms"
      }).
    when('/forms', {controller:FormsIndexCtrl,
      templateUrl:'<%= asset_path("forms/index.html") %>'}).
    when('/forms/new', {controller:FormsCreateCtrl,
      templateUrl:'<%= asset_path("forms/new.html") %>'}).
    when('/forms/:id', {controller:FormsShowCtrl,
      templateUrl:'<%= asset_path("forms/show.html") %>'}).
    when('/forms/:id/edit', {controller:FormsEditCtrl,
      templateUrl:'<%= asset_path("forms/edit.html") %>'}).otherwise({
        redirectTo: "/"
      });
    }
  ]);

  root.thisApp = thisApp;

}).call(this);
